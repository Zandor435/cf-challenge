# Overnight Audit & Fix Run — 2026-08-21

Branch: `overnight-audit-2026-08-21` (off `origin/main` @ `6a23196`). Main untouched.

## 0. Ground truth snapshot (Phase 0, read-only)

### 0.0 Where the run actually happened — IMPORTANT
The prompt named `c:/GitHub/Personal Git/CF Challenge`; that path does not exist
on this machine. The only clone of `Zandor435/cf-challenge` is
`c:\Users\zacha\Claude Code\cf-challenge`, and it is **shared with other live
Claude Code sessions** (`ListAgents` showed four interactive `cf-challenge`
sessions; one of them rewrote `scripts/preseason_baseline.py` in the working
tree — 268 → 826 lines, briefly with a SyntaxError — while this run's first
`pytest` was executing). Checking the shared tree out to the audit branch would
have moved those sessions onto it, so:

- the shared checkout was put back on `main` (their uncommitted edit carried
  across untouched; `git status` there still shows ` M scripts/preseason_baseline.py`),
- the audit branch was moved into an isolated **git worktree** at
  `c:\Users\zacha\Claude Code\cf-challenge-overnight` (CLAUDE.md P3 §18), and
  every commit below was made there.

`/output/` is gitignored, so the worktree has no `output/` tree; the 0.9 checks
below read the shared checkout's `output/` in place and wrote nothing.

### 0.1 Repo state
```
$ git log --oneline -20
6a23196 chore(panel): commit the other three boards from the same draft run
b3ef1cb chore(panel): commit the standings regenerated by the real draft
9b82684 fix(validate): the waiver abort fires only on waiver-config errors
e073940 feat(panel): enter the real 2026 draft, replacing dummy sample data
47c3832 fix(panel): conference minimum 4->3 (config drift); record Chris waiver
43bc9da feat(art): the fat-coach generator, which never existed in code
ae546c4 feat(validate): per-manager conference-minimum waivers, announced in every run
f466262 feat(browns): add Josh and Marc to the roster
8d69deb feat(art): panel banner regenerated textless; SVP staged for all four
5eefd2d feat(art): every group has a comic banner; family needed two panels
d8e0a86 feat(art): profile pictures for all four groups -- 22 avatars, 18 new
49c177e feat(art): style, composition, character and locks become four separate parts
3c9586a feat(browns): hero banner, and proof the generator ports across groups
2f4cf36 feat(art): a --ref may carry a hex, and color_name learns brown
d52aef5 feat(art): SVP is shared by every group, not church's
68193f0 feat(art): the selection sheet files every group, and a gap report
81c20cd refactor(art): one portrait per manager, at a path that names its group
53f09c5 ci(workflows): bump five action pins off the deprecated Node 20 runtime
4a5bf96 style(scenes): wrap the archive glob to the file's 79-column width
6f746ad fix(personas): the selection record's published flags match the site again
$ git status            (at start, shared checkout)
On branch main
Your branch is up to date with 'origin/main'.
nothing to commit, working tree clean
$ git rev-parse HEAD origin/main
6a23196c95025bf2cc5f3e20e556a354099e294b
6a23196c95025bf2cc5f3e20e556a354099e294b
```

### 0.2 Season state — the backlog was stale here
`season.json` = `{"season": 2026, "cfbd_default_season": 2026}` (already flipped;
the "Going live" checklist step 3 in README is done).

| group  | draft_status | picks | managers |
|--------|--------------|-------|----------|
| panel  | **final**    | 16 real | jonathan, zach, blaine, chris (4 each) |
| family | dummy        | 32    | 8 managers × 4 |
| church | dummy        | 20    | 5 managers × 4 |
| browns | dummy        | 28    | 7 managers × 4 (josh, marc added in f466262) |

Panel's real draft was entered today (e073940). Its config is
`min_distinct_conferences: 3` with a waiver for chris (2 conferences) — the
§9 gate prints `WAIVED: [panel] chris …` on every run.

### 0.3 Browns parity
Complete: `groups/browns/{config,personas,personas.md,picks}.json`,
`docs/data/browns/{analytics,personas,projection,standings,timeline,timeline-2025}.json`,
banner, art_slots entry, 5/7 profile heroes + ripped cuts + avatars. No
"browns deferred" flag or shim anywhere in code (grep of py/md/yml/js/html/json
for `deferred` finds nothing). Residual drift is **documentation only** (see 0.12)
plus an **art gap** for the two managers added today:

```
$ python scripts/art_gaps.py        (offline; excerpt)
GAP    10   needs generation
  browns josh / marc: profile_hero, page_hero, avatar  (no source art in output/personas/browns/)
  editorial_hero: GAP for all four groups (slot declared empty — by design)
NOTE: browns has 7 managers, over the 5-reference ceiling for one render.
      Its published banner therefore CANNOT show all 7 -- it is stale by construction.
```
`assets/portraits/index.json` has no `josh`/`marc` entry, so the site renders
initials, not 404s (app.js gates avatars on that manifest). Generating the art is
a paid Gemini call → **not done tonight** (hard rule 2).

### 0.4 Test baseline (clean HEAD, isolated worktree)
```
$ python -m pytest -v -p no:cacheprovider
platform win32 -- Python 3.12.0, pytest-9.1.1
collected 95 items
... (95 × PASSED; every file: test_analytics_week_move, test_cache_access,
test_commentary_prompt, test_enter_draft, test_fetch_integrity, test_output_shape,
test_pick_rules, test_preseason_baseline, test_projector_correlation,
test_projector_h2h, test_resolver, test_should_run, test_style_spec,
test_suite_collection, test_timeline_seasons, test_week_packet)
============================= 95 passed in 2.60s ==============================
```
No failures, no skips. (The very first run, in the *shared* checkout, hit a
collection error in `scripts/preseason_baseline.py:786` and then a
`test_cache_access::test_raw_banked_index_ownership` failure — both caused by
the other session's uncommitted rewrite of that file, not by anything committed.)

Also run offline, both exit 0:
```
$ python scripts/validate_team_names.py
  WAIVED: [panel] chris — conference minimum (2 of 3 required: ['Big Ten', 'SEC']) — commissioner waiver, 2026 season — drafted 2 conferences [granted 2026-08-21]
Gate OK: 96 real pick(s) resolved + conference-checked + draft-rule-checked across 4 group(s).
$ python scripts/sync_personas.py --check
ok    [browns] [church] [family] [panel]: docs/data/<g>/personas.json matches source
```

### 0.5 CI config
- `tests.yml`: `actions/checkout@v7`, `actions/setup-python@v7` (python 3.12);
  steps: `python -m pytest -v` → `validate_team_names.py` → `sync_personas.py --check`
  → `selftest_10_1.py` (**the only `continue-on-error: true`**, justified in a
  long comment as "trips the 10-day cache-staleness ceiling").
- `update-data.yml`: `continue-on-error` on the CFBD fetch and on the
  week-packet build — both deliberate playbook-rule-3 degradations, left alone.
- `pages.yml`: configure-pages@v6, upload-pages-artifact@v5, deploy-pages@v5.
- **There is no `actions/setup-node` and no `node-version` pin in any workflow.**
  53f09c5 (today, 17:34) already moved every action to a Node-24 major.
  Local `node --version` = v20.10.0.

### 0.6 selftest_10_1.py
`fetch_results.py --simulate-failure` raises before any client/key is touched
(zero network), then `degraded_exit()` exits 0 if the cache is same-season and
≤ `STALE_DAYS=10`, else 4. The committed cache is `fetched_at 2026-08-21T14:51Z`,
so the staleness ceiling is **not** what fails today. Run offline:
```
$ python scripts/selftest_10_1.py
  [FAIL] FBS teams have 12/13 scheduled games — 137 FBS teams; outliers={'Fresno State': 11, 'Oregon State': 11, 'Texas State': 11, 'Boise State': 11, 'Colorado State': 11, 'Utah State': 11, 'San Diego State': 11, 'Washington State': 11}
  [FAIL] flag OFF: all 136 FBS teams sit at 12 — 137 FBS teams this season ...; !=12: {... 'San José State': 13 ...}
  [FAIL] flag ON: exactly 18 FBS teams sit at 13 — 1: ['San José State']
  [FAIL] the 18 are exactly the 9 conference title-game pairings — 0 title games, 0 FBS participants
  (all 16 resolver / failure-path / season-guard checks PASS; exit=3 on the wrong-season path)
RESULT: 20/24 checks passed
```
So the real dependency is **season state**: the 2026 CFBD slate has 0 conference
title games posted and the Pac-12 schedule is incomplete (8 teams at 11), and
the census hardcodes 136/18/9 from the completed-2025 cache. Fixable offline —
see 1.3.

### 0.7 Opposite-side enforcement — already a hard gate
`validate_team_names.py:213-255` (`validate_group_data`) already fails
`same-team-same-side` via the `err()` helper over `_real_picks()`, within group,
keyed on canonical team + normalized direction; `duplicate-team` covers one
manager holding a team twice. `_real_picks()` skips only `_note` rows and TODO
placeholders — **`draft_status` is never consulted, so dummy picks are not
exempt**. `test_pick_rules.py::test_team_sharing_gate` already pins (a) same
side fails and (b) opposite sides pass. The slug in the backlog
(`duplicate-same-side`) does not exist; the established slug is
`same-team-same-side`. `build_week_packet.py:355-375 detect_feuds()` still has
the stderr `::warning::` for the same condition. Gap = tests (c) cross-group and
(d) dummy-not-exempt were not pinned. CI runs the gate on committed picks and it
passes today (0.4).

### 0.8 Gemini budget counter — undercount confirmed
`generate_owner_images.py`: `bump_budget(budget, provider)` is called **once per
image, before** `gen_*()`, which calls `_post_with_retries(do_post)`. The shell
keeps two independent retry lists — `TRANSIENT_WAITS=[5,10,20]` and
`RATE_LIMIT_WAITS=[60,60,60]` — so one image can cost **up to 7 HTTP requests**
(1 + 3 transient + 3 rate-limited) while the tally records 1. Same pattern in
every caller: `generate_banners.py:337`, `generate_scenes.py:834`,
`recolor_personas.py:291`, `make_fatcoach.py:180`, and `gemini_image.generate()`
(no counting at all — callers count for it). `derive_style.py` counts 1 per call
and goes through `generate_commentary.call_openai`, not this shell (out of scope).
Correct unit: **one bump per `do_post()` attempt**, i.e. inside the shell.

### 0.9 "77-keep sweep" — never ran, and the keep list is not in the repo
- `output/rejected/` does not exist (shared checkout; `find` → 0 files).
- Nothing in the repo mentions "77", a keep list, or `output/rejected`.
- `scripts/build_selection_sheet.py` keeps its keep/chuck marks in **browser
  localStorage**; the handoff is a copied "KEEP list" or a downloaded
  `selection.json` — neither exists under `output/` or `data/`.
- `data/persona_selections.json` records only the **panel 2026-08-18** decisions,
  and that sweep *did* run (rejects in `output/archive/...`, 2026-08-21 bookkeeping note).
- Disk counts (shared checkout): 292 files under `output/`; per-dir top:
  scenes/panel 65, posters/panel 28, matchups/panel 18, personas/{panel 5, church 5,
  family 8, browns 6}.

→ Phase 1.6 condition ("keep-list is unambiguous") is not met; no sweep script was
written or run. See "Needs Z's call".

### 0.10 JSON contract validation — clean
- 49/49 JSON files under `data/`, `groups/*/`, `docs/data/*/`, `season.json` parse.
- All four `docs/data/<g>/analytics.json` carry exactly `race, championship_odds,
  best_worst, paths, portfolio` + `leverage: null` + `meta`.
- Every pick in all four groups: `conference` and `line` match
  `data/team_win_totals_2026.json`; every `direction` ∈ {O, U}.
- Every persona id in `groups/<g>/personas.json` matches `config.json`; every
  declared art path in `art_slots.json` / `heroes.json` / `portraits/index.json`
  exists on disk **except** the browns josh/marc gap in 0.3 (group-default
  candidates for two managers who have no art yet; avatars are manifest-gated so
  no 404 is emitted; the profile hero for those two is checked in Phase 2).

### 0.11 Frontend static pass
```
$ for f in docs/**/*.js: node --check
OK docs/analytics.js  OK docs/app.js  OK docs/managers.js  OK docs/site.js
```
Every `href`/`src` in `docs/*.html` resolves (the one hit, `svp.html:73 href="' + page + '"`,
is an inline-JS template, not a path). Inter-page links: index ↔ managers ↔
analytics ↔ svp all present.

### 0.12 Docs drift (claims that no longer match the repo)
README.md:
1. Groups table lists panel/family/church only — **browns missing**.
2. "All three groups currently ship engineered dummy picks" — four groups; panel is `final`.
3. Data-flow diagram writes `site/data/<group_id>/` — the real, contract-locked
   target is `docs/data/<group_id>/` (`docs/output-contract.md:5`).
4. "`null` = unenforced" for the draft rules — the validator has **no unenforced
   path**; a missing key is a `rules-missing` failure.
5. "EXACTLY 4 per manager across 4 distinct conferences" — per-group config;
   panel is 4 picks / ≥3 conferences with a waiver mechanism.
6. "Tests (all part of the suite)" lists eight `python scripts/…` commands — the
   suite is `python -m pytest` (95 items) + the two gate scripts.
7. "Going live" step 3 ("flip season.json to 2026") is already done.

ARCHITECTURE.md:
8. §5 "writes … **only** to `site/data/<group_id>/`" and "The old `groups/*/output/`
   directory is removed" — target is `docs/data/`, and `groups/<g>/output/` is
   live (commentary columns + `column_memory.json`, see .gitignore and
   `build_week_packet.py:108`).
9. §5 tree shows three groups; §1 "3–4 independent friend groups".
10. §1 / §5 "EXACTLY 4 … from 4 distinct conferences (LOCKED)" — see README #5.
    (Judgment: the LOCK was changed by Z in 47c3832; reported, not rewritten.)

Elsewhere (comments, not docs — reported only): `docs/site.js:208` and
`docs/managers.js:400` "All four groups are dummy today"; `docs/assets/art_slots.json`
`$note` "for all three groups"; `groups/*/config.json` `_note` "output path
site/data/<group_id>/"; `.gitignore` "EXCEPT the three real groups" (lists four);
`tests.yml` comment "91 items" (95). `CLAUDE.md` is the generic playbook and makes
no repo-specific claims — no drift.

### 0.x Extra findings surfaced during Phase 0
- `docs/data/family/standings.json` and `docs/data/church/standings.json` carry
  `meta.cache_fetched_at = 2026-08-21T13:14:07Z`, while panel/browns (and the
  committed cache) are `14:51:21Z`. 6a23196 says "commit the other three boards
  from the same draft run", but family/church were scored off the earlier cache.
  Preseason (`as_of_week: null`), so numbers are unlikely to differ — not
  regenerated tonight (it rewrites append-only timelines; Z's call).

---

## 1. Commits landed — `git log --oneline main..overnight-audit-2026-08-21`
```
ef0d31c fix(requirements): add Pillow -- the Tests workflow has been red on main since f466262
fd4640b docs(report): overnight audit 2026-08-21 -- snapshot, commits, findings, Z's calls
584f20a docs: README/ARCHITECTURE match the repo (four groups, docs/data, real draft)
4cd7809 ci(selftest): make selftest_10_1 honest offline and drop continue-on-error
c9e2df1 fix(budget): count every HTTP attempt, not one per image
00c4c90 test(pick-rules): pin the sharing gate's per-group scope and no-dummy-exemption
```
(+ the final report commit on top.) Review: `git diff main..overnight-audit-2026-08-21`.
Every commit message carries the full what/why; this section is the test evidence.

### 00c4c90 — test(pick-rules)  [Phase 1.1]
Backlog item was already done in code (0.7); only the untested corners were
pinned. `scripts/test_pick_rules.py` +1 test (3 checks). `build_week_packet.py`
stderr warning kept (reason in the commit message).
```
$ python -m pytest scripts/test_pick_rules.py -v
scripts/test_pick_rules.py::test_sharing_gate_is_per_group_and_has_no_dummy_exemption PASSED
============================== 5 passed in 0.05s ==============================
$ python scripts/test_pick_rules.py
  [PASS] same team + same side in two DIFFERENT groups passes both gates — one=[] two=[]
  [PASS] draft_status "dummy" still fails same-team-same-side — errors=[('a & b', 'Ohio State', 'same-team-same-side')]
  [PASS] the dummy group reports exactly what the final group reports — dummy=[...] final=[...]
RESULT: 23/23 checks passed
$ python -m pytest -q          -> 96 passed in 1.86s
```

### c9e2df1 — fix(budget)  [Phase 1.2]
`_post_with_retries(do_post, budget=, provider=, daily_warn=)` counts before
EVERY attempt; 6 call sites thread the budget through instead of pre-counting;
`scripts/test_budget_counter.py` (5 tests, 18 checks) with a scripted transport,
temp budget file, `time.sleep` stubbed. **No live call.**
```
$ python scripts/test_budget_counter.py
  [PASS] the tally says 3, not 1 — tally={'gemini': 3, 'openai': 0}
  [PASS] ::warning:: printed exactly once (attempt 3 is the first past 2)
  [PASS] tally says 4 — tally={'gemini': 4, 'openai': 0}
  [PASS] gen_gemini billed 2 on gemini … gen_openai billed 2 on openai … gemini_image.generate billed 2 more on gemini (4 total)
  [PASS] no budget file was written
RESULT: 18/18 checks passed
$ python -m pytest -q          -> 101 passed in 1.89s
$ ls data/gemini_budget.json   -> No such file or directory   (real tally untouched)
```
**Needs live verification:** on the next real generation run, the tally should
climb by more than the image count whenever a retry line is printed.

### 4cd7809 — ci(selftest)  [Phase 1.3]  — path taken: *fixable offline*
The stated reason for `continue-on-error` (stale-cache ceiling) was not the
live failure (0.6). Census gated on title games (SKIP with reason), expected
counts derived from the cache (no 136/18/9), bypass test asserts whichever
`degraded_exit` branch the cache is in. `continue-on-error` **removed** from
`tests.yml`; conftest/test_suite_collection rationale corrected;
`scripts/test_selftest_census.py` (3 tests, 13 checks) on synthetic caches.
```
$ python scripts/selftest_10_1.py                 (committed cache, 0.5d old)
  [SKIP] schedule census (12/13 per FBS team; title-game participants at 13) — the season-2026 cache holds 0 conference title games -- CFBD has not posted them yet, so the slate is not complete enough to census. Runs again on its own once a fetch brings them in.
  (cache is 0.5d old, within the 10-day ceiling -- asserting the degraded-but-running branch)
RESULT: 20/20 checks passed, 1 skipped (reasons above)      exit=0
$ (temp shadow repo, cache fetched_at pushed back 30 days)
  (cache fetched_at is 30.0d old, past the 10-day ceiling -- asserting the fail-loud branch)
  [PASS] stale fallback exits 4 (refuses to score stale data) — exit=4
  [PASS] error names the staleness ceiling
RESULT: 20/20 checks passed, 1 skipped (reasons above)      exit=0
$ python scripts/test_selftest_census.py
  [PASS] exactly one census skip was recorded
  [PASS] every check passed (4 basic + 4 census)   [9 title games -> "exactly 18 FBS teams sit at 13"]
  [PASS] 7 title games passes too (no hardcoded 18/9)
  [PASS] the census recorded at least one FAIL     [one team at 11 -> "!=12: {'Wyoming': 11}"]
RESULT: 13/13 checks passed
$ yaml.safe_load(tests.yml) -> steps: [Checkout, Setup Python, Install dependencies, pytest (whole suite), validate_team_names.py (§9 gate), sync_personas.py --check, selftest_10_1.py (cache health + bypass/season-guard paths)]; continue-on-error present: False
$ python -m pytest -q          -> 104 passed in 1.89s
```
Disclosure: this commit was **amended once before push** (c54e0e7 → 4cd7809).
The first message claimed the census had been proven on the 2025 contract
fixture; that run had actually exited 1 because the fixture is a 12-team slice
with no `counts` key (it cannot run this script at all — pre-existing). The
amend added the synthetic-cache test and rewrote the claim truthfully. Nothing
had been pushed.

### 584f20a — docs  [Phase 1.7]
README + ARCHITECTURE fact corrections only (items 1–9 of 0.12). Item 10
(the "EXACTLY 4 / 4 conferences (LOCKED)" wording) deliberately untouched.
No tests affected: `104 passed`.

### ef0d31c — fix(requirements)  [Phase 1.5, found by CI after the push]
The first dispatched CI run on the branch failed in pytest — **and so had the
last four pushes to `main`** (f466262, 43bc9da, b3ef1cb, 6a23196), identically:
```
$ gh run view 32549756553 --log-failed      (branch)      / 32548376590 (main, 6a23196)
scripts/test_style_spec.py::test_style_spec FAILED
  scripts/style_spec.py:99: in _banned_terms -> scripts/generate_scenes.py:53 -> scripts/recolor_personas.py:49
E   ModuleNotFoundError: No module named 'PIL'
======================== 1 failed, 103 passed in 2.17s =========================
```
Eight scripts import `PIL` unconditionally; `requirements.txt` never listed
Pillow; the workstation has it, runners don't. One line added (no pin, matching
the file). Local: `104 passed`. CI re-dispatched → green (see §5).

### Skipped phases, with reason
- **1.4 Node 20 → 24:** nothing to bump — no `setup-node`/`node-version` in any
  workflow; action pins already on Node-24 majors since 53f09c5. `node --check`
  clean locally (v20.10.0). The green CI run in §5 executed on those pins with
  no runtime-deprecation annotation.
- **1.5 reds / contract violations:** none locally (0.4, 0.10, 0.11 all clean);
  the one CI-only red (Pillow) is fixed in ef0d31c above.
- **1.6 77-keep sweep:** precondition not met — the keep list does not exist in
  the repo (0.9). No script written, nothing moved. See §3/§4.

## 2. Frontend screenshot gate (Phase 2)
Served the worktree's `docs/` on `127.0.0.1:8765`; Playwright (python) captured
**17 pages × 2 widths (1440, 390), full page**, with console, page-error,
HTTP≥400, broken-image, horizontal-overflow, sample-banner and tone-block
probes. Captures + machine report (not committed, gitignored `/output/`):
`c:\Users\zacha\Claude Code\cf-challenge\output\overnight_screenshots\<page>-<width>.png`
and `_report.json`. Pages: `landing`, and per group `<g>-standings`,
`<g>-profiles`, `<g>-analytics`, `<g>-svp`.

Summary (identical at both widths):
```
page                  banner imgs console http>=400 overflow
landing / panel-*       -     53     0       0        no
family-*                Y    106     0       0        no
church-*                Y     67     0       0        no
browns-standings        Y     82     0       0        no
browns-profiles         Y      5     2       2        no   <- assets/profiles/browns/{josh,marc}-ripped.webp 404
browns-analytics        Y      -     0       0        no
*-svp                   -      0     0       0        no   (static stub, see F3)
```
- **Dummy banner correct per group:** shown on family/church/browns standings,
  profiles and analytics; absent on panel (`draft_status: final`).
- **Tone gate correct:** case-insensitive DOM probe per manager —
  family straight-tone `john`, `rachel`, `vic` render **no** Fatal-flaw /
  Running-gag blocks; every roast/warm manager with the field populated does
  (panel 4×flaw+gag; family gayden flaw+gag, devin/holly/gunner/zach gag only —
  their `fatal_flaw` is null in the data; church 5×gag; browns 5×gag, josh/marc
  none — no persona prose yet).
- **No horizontal overflow at 390** on any page (`scrollWidth == innerWidth`).
- **No JS errors** anywhere except the two 404s above.
- **Blank hero boxes** for below-the-fold managers in the full-page captures
  (family gunner/gayden/rachel/vic/john, church brian/john_wells/zach, browns
  todd/hauck/matt_m) are a **capture artifact**: every `<img>` reports
  `complete && naturalWidth>0` before any scroll, and a scrolled re-capture
  (`<g>-profiles-1440-scrolled.png`) paints all of them. Not a site defect.
- **Fixes made in Phase 2: none** — every visual finding is an art gap (paid
  generation) or product judgment. See §3.

## 3. Findings, not fixed
| # | Finding | Evidence | Recommended next action |
|---|---------|----------|------------------------|
| F1 | **Browns josh/marc have no art**: `profile_hero`, `profile_page_hero`, avatar all GAP; managers.html 404s `josh-ripped.webp` / `marc-ripped.webp` (the page copes — name + picks table only; initials avatar on the board). | 0.3, `art_gaps.py`, browns-profiles capture | Fat-coach generator for the two (paid Gemini — Z's call), then `prepare_portraits.py` / `build_avatars.py --group browns` / `build_profile_heroes.py`. |
| F2 | **Browns banner stale by construction** (7 managers > 5-ref ceiling; 5 in the render). Family (8) has the same note. | `art_gaps.py` NOTE lines | Composited two-panel rebuild, as family's was. Paid. |
| F3 | **`docs/svp.html` is a static stub** with hardcoded fiction — "Rachel banked her fourth clinch before October…" and a `Week 01` masthead label — identical for all four groups, including panel whose draft is real and whose board says "drafted — no games played". The index card says "coming soon" and links here as "the column preview". | `docs/svp.html:38,55-57`; `*-svp-1440.png` | Product call: neutral placeholder copy with the week label read from standings meta, or keep as preview. One-file HTML edit once decided. |
| F4 | `docs/data/family/standings.json` and `church/standings.json` scored off the **13:14Z** cache; panel/browns off **14:51Z** (the committed one), despite 6a23196's "same draft run". Preseason, so numbers almost certainly identical. | 0.x | `python scripts/run_groups.py --group all` (offline) and commit `docs/data/` — or let the first cron run rewrite all four. |
| F5 | ARCHITECTURE §1/§5 still say "EXACTLY 4 … from **4** distinct conferences (LOCKED)"; panel runs 4 / ≥3 + a waiver since 47c3832. | 0.12 #10 | Z rewrites the lock in Z's words — it is a league-rule decision, not a typo. |
| F6 | `groups/*/config.json` `_note` says `site/data/<group_id>/`; `docs/site.js:208` and `docs/managers.js:400` say "All four groups are dummy today"; `art_slots.json` `$note` says "all three groups"; `.gitignore` says "three real groups"; `tests.yml` comment says "91 items". | 0.12 | Cosmetic; batch into the next touch of each file. |
| F7 | 390px nav: the group tab strip wraps "THE BROWNS" to a second row and the page tabs wrap "ANALYTICS". Legible, no overflow. | `landing-390.png` | Design taste — leave, or tighten tab padding. |
| F8 | `selftest_10_1`'s census-on-real-data branch cannot be exercised offline: no full completed-season cache is in the repo any more (the 2025 one was replaced at the season flip; the contract fixture is a 12-team slice without `counts`). Covered by the synthetic test only. | 1.3 | Consider committing a full-cache fixture the next time a completed season is on disk (~2 MB). |
| F9 | The shared working tree holds an **uncommitted 826-line rewrite of `scripts/preseason_baseline.py`** from another session (briefly a SyntaxError; makes `test_cache_access::test_raw_banked_index_ownership` fail there). Not mine, not touched. | 0.0, 0.4 | Check with that session before committing anything in the shared checkout. |

## 4. Needs Z's call
1. **Merge** `overnight-audit-2026-08-21` (4 commits + this report). Nothing merged; main untouched.
2. **Live verification** of c9e2df1 (budget counter) on the next real image-gen run: tally ≥ images, and it grows on retry lines.
3. **77-keep sweep**: no keep list exists in the repo. Export the selection sheet's KEEP list ("Copy KEEP list" / downloaded `selection.json`) into the repo, then a sweep script can be written against it. Nothing was moved tonight.
4. F1/F2 art generation (paid), F3 svp stub copy, F4 board regen, F5 lock wording.
5. The shared checkout's foreign uncommitted change (F9).
6. The sibling worktree `c:\Users\zacha\Claude Code\cf-challenge-overnight` can be removed after review (`git worktree remove cf-challenge-overnight`).

## 5. Baseline vs final test run
| run | items | result |
|-----|-------|--------|
| baseline (clean HEAD 6a23196, worktree) | 95 | `95 passed in 2.60s` |
| final (584f20a) | 104 | `104 passed in 1.95s` |
| delta | +9 | test_pick_rules +1, test_budget_counter +5, test_selftest_census +3 |

Baseline raw tail:
```
scripts/test_week_packet.py::test_packet_schema PASSED
============================= 95 passed in 2.60s ==============================
```
Final raw tail (`python -m pytest -v -p no:cacheprovider`):
```
scripts/test_week_packet.py::test_packet_schema PASSED
============================= 104 passed in 1.95s =============================
$ python scripts/validate_team_names.py  -> Gate OK: 96 real pick(s) ... across 4 group(s).
$ python scripts/sync_personas.py --check -> ok [browns] [church] [family] [panel]
$ python scripts/selftest_10_1.py        -> RESULT: 20/20 checks passed, 1 skipped (reasons above)
$ node --check docs/{analytics,app,managers,site}.js -> OK ×4
```
No test was removed, skipped, or xfailed in the pytest suite; the one SKIP is
inside `selftest_10_1.py`, printed with its reason, and is the deliberate
replacement for CI's `continue-on-error`.

### CI on the branch (`Tests`, `workflow_dispatch` — a branch push alone does not trigger it)
| run | head | result |
|-----|------|--------|
| 32549756553 | fd4640b | **failure** — `1 failed, 103 passed` (`No module named 'PIL'`, pre-existing on main) |
| 32549822422 | ef0d31c | **success** — all four steps, 14s |

Raw lines from the green run (`gh run view 32549822422 --log`):
```
[pytest (whole suite)] ============================= 104 passed in 1.98s ==============================
[validate_team_names.py (§9 gate)] Gate OK: 96 real pick(s) resolved + conference-checked + draft-rule-checked across 4 group(s).
[sync_personas.py --check (docs/ copy is current)] ok [browns] [church] [family] [panel]: docs/data/<g>/personas.json matches source
[selftest_10_1.py (cache health + bypass/season-guard paths)]   [SKIP] schedule census (12/13 per FBS team; title-game participants at 13) — the season-2026 cache holds 0 conference title games -- CFBD has not posted them yet, so the slate is not complete enough to census. Runs again on its own once a fetch brings them in.
[selftest_10_1.py (cache health + bypass/season-guard paths)] RESULT: 20/20 checks passed, 1 skipped (reasons above)
```
The selftest step ran **without** `continue-on-error` and passed on its own
merits. No Node-runtime deprecation annotation appeared on the run.
`main`'s `Tests` workflow stays red until ef0d31c (or an equivalent) lands there.
